includes:
    - vendor/larastan/larastan/extension.neon
    - vendor/nesbot/carbon/extension.neon

parameters:
    level: 5

    paths:
        - src/

    ignoreErrors:
        # This package exposes traits for consumers; they may not be used inside this repo.
        - identifier: trait.unused
