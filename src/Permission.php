<?php

declare(strict_types = 1);

namespace JuniorFontenele\LaravelPermission;

use JuniorFontenele\LaravelPermission\Support\PermissionManager;

// Kept for BC with skeleton "PackageName" entry point.
// This class is the main public API (facade targets it).
final class Permission extends PermissionManager
{
    // Intentionally empty; extends PermissionManager.
}
